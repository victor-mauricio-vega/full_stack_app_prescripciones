model Doctor {
  id        String @id @default(cuid())
  specialty String

  // Dueño de la FK hacia User.
  user   User?    @relation("UserDoctor", fields: [userId], references: [id])
  userId String? @unique

  // Recetas firmadas por este doctor
  prescriptions Prescription[] @relation("AuthoredByDoctor")

}
